#include "../include/rules.h"

	
bool isCheck(char color, const Board& board){
	// TODO: iterate over the board
		// if a piece is of opposite color
		// generate it's legal moves and 
		// see if any of them are capturing the 
		// king

	return false;
}

bool isCheckmate(char color, const Board& board){
	// TODO: if isCheck and king has no legal moves

	return false;
}
